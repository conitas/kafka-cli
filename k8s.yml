apiVersion: v1
kind: Pod
metadata:
  name: kafka-cli
  labels:
    purpose: kafka-cli
spec:
  containers:
    - name: kafka-cli
      image: itasgmbhde/kafka-cli
      command: ["/bin/sh"]
      args: ["-c", "while true; sleep 3600; done"]
  restartPolicy: OnFailure
